{
  "name": "@increa/pdf-reader-mcp",
  "version": "0.1.0",
  "description": "MCP server for PDF reading (Python package)",
  "private": true,
  "scripts": {
    "dev": "../server/.venv/bin/python pdf_reader_server.py",
    "test": "pytest tests/ -v",
    "test:cov": "pytest tests/ -v --cov=pdf_reader_server",
    "lint": "../server/.venv/bin/ruff check .",
    "format": "../server/.venv/bin/ruff format ."
  }
}
